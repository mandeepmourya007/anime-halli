services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_SITE_URL=${NEXT_PUBLIC_SITE_URL:-http://localhost:3000}
      - ANILIST_ENABLED=${ANILIST_ENABLED:-false}
    restart: unless-stopped
